PORT=3000
NODE_ENV=development

DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=internetperla
DB_SYNC=true

JWT_SECRET=supersecretjwt
JWT_EXPIRES_IN=7d
BUSINESS_TZ=America/Guatemala
AUTO_CLOSE_ENABLED=true

# For cloud deployments (Neon, Render, etc.) - SOLO EJEMPLO
# NO uses credenciales reales aquí, esto se sube a GitHub.
# Usa sslmode=require en Neon/Render
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require
# Nota: cambiar JWT_SECRET invalida tokens existentes; obliga a re-login
